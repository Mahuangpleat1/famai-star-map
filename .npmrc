# npm 配置 · 法脉星河

# 锁文件必须 commit
package-lock=true

# 安装时禁止自动修改 lock
# (避免不同人 install 后 lock 漂移)

# 精确到小版本(dev 库,避免大版本升级破坏构建)
save-exact=false

# 节省带宽
fund=false
audit=false

# 安装行为
legacy-peer-deps=false
prefer-online=false
